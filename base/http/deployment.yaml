containers:
- name: http
  image: python:3.11
  workingDir: /app
  command: ["sh", "-c"]
  args:
    - pip install nats-py flask && python app.py
  ports:
    - containerPort: 5000
  volumeMounts:
    - name: app-code
      mountPath: /app