containers:
- name: worker
  image: python:3.11
  workingDir: /app   # 🔥 same as http-service
  command: ["sh", "-c"]
  args:
    - pip install nats-py && python worker.py
  volumeMounts:
    - name: worker-code
      mountPath: /app